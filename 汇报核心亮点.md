# 区域树节点管理系统 — 汇报核心亮点

> 本文档面向明日技术演示汇报，聚焦五个核心设计亮点，每个亮点配有"是什么 → 为什么 → 怎么做到的"三段说明及真实数据示例。

---

## 亮点一：闭包表（Closure Table）替代递归查询

### 是什么

系统在节点表（`area_nodes`）之外，额外维护了一张**闭包关系表**（`area_closures`）。
这张表记录树中**所有祖先-后代对**，包含节点自身（深度为 0）。

### 为什么

传统树查询用递归：查"机电产业园"的全部子孙，需要先查直接子节点，再对每个子节点再查，层层递归，查询次数 = 树的深度。

闭包表的思路是：**把递归的计算成本，转移到写入时一次性计算好，查询时直接扫描一张平铺表**。

| 操作 | 递归方案 | 闭包表方案 |
|------|---------|-----------|
| 查完整子树 | N 次递归查询（深度 N） | 1 次扫描，过滤 `ancestor_id = 目标` |
| 查是否有子节点 | 查子节点数量 | 直接读 `has_children` 字段 |
| 删除子树 | 递归遍历再逐条删 | 先从闭包取 ID 集合，批量软删 |

### 真实数据示例

以"机电产业园"（id=1）为例，在 `area_closures` 中的记录：

```
ancestor_id=1, descendant_id=1, depth=0   ← 自身
ancestor_id=1, descendant_id=5, depth=1   ← 停车场
ancestor_id=1, descendant_id=2, depth=2   ← 监控室
ancestor_id=1, descendant_id=3, depth=3   ← 内监控室
ancestor_id=1, descendant_id=4, depth=4   ← 火灾监控
ancestor_id=1, descendant_id=7, depth=5   ← 给个
ancestor_id=1, descendant_id=8, depth=6   ← 测试子节点这个
```

查询"机电产业园"完整子树，只需一行逻辑：

```typescript
// treeService.ts — listSubtree()
for (const c of this.db.data.area_closures) {
  if (c.ancestor_id === nodeId) desc.add(c.descendant_id);
}
// 然后用 desc 集合过滤 area_nodes，一次扫描搞定
```

**无论树有多深，查询逻辑永远是这一段，不会增加层数。**

---

## 亮点二：path_name 物化路径 — O(1) 读取完整路径

### 是什么

每个节点的 `path_name` 字段，在**创建/重命名/移动时**就已经计算好并保存，是一个用 `/` 分隔的完整可读路径字符串。

### 为什么

业务上经常需要展示"当前节点在哪"，例如面包屑导航。
如果每次都从节点一路向上追溯父节点，需要 N 次查询（N = 深度）。

`path_name` 是**以写换读**的典型设计：接受写入时多做一点级联更新，换取读取时的零查询成本。

### 真实数据示例

```json
{
  "id": 8,
  "name": "测试子节点这个",
  "path_name": "机电产业园/停车场/监控室/内监控室/火灾监控/给个/测试子节点这个"
}
```

节点深度为 6 层，但读完整路径只需：

```typescript
// treeService.ts — getPathName()
getPathName(id: number): string | undefined {
  return activeNode(this.db, id)?.path_name;  // 直接字段读取，O(1)
}
```

前端 QueryPanel 也有专门的「完整路径」Tab，Tooltip 提示写的就是：**直读 path_name 字段，O(1)**。

### 级联维护逻辑

重命名或移动时，系统会自动级联更新所有后代的 `path_name`：

```typescript
// renameNode() — 重命名时级联更新所有子孙的 path_name
for (const n of this.db.data.area_nodes) {
  if (n.path_name.startsWith(oldPrefixSlash)) {
    n.path_name = newPrefixSlash + n.path_name.slice(oldPrefixSlash.length);
  }
}
```

---

## 亮点三：软删除 — 数据不丢失，可审计

### 是什么

删除操作不会物理移除任何数据，只是把节点（及其整棵子树）的 `is_active` 字段从 `1` 改为 `0`。

### 为什么

物理删除是不可逆的。生产环境中，区域数据往往与设备、告警、工单等业务数据关联，直接删除会破坏引用完整性，并且无法追溯历史。

软删除的好处：
- **可恢复**：`is_active` 改回 1 即可恢复
- **可审计**：完整保留数据变更历史
- **关联安全**：其他系统引用的 ID 不会失效
- **子树联动**：删除父节点，子树全部一起标记为非激活，不会出现"孤儿节点"

### 真实代码

```typescript
// treeService.ts — softDeleteSubtree()
softDeleteSubtree(nodeId: number): number {
  const ids = subtreeIds(this.db, nodeId);  // 从闭包表取全部后代 ID
  let n = 0;
  for (const id of ids) {
    const node = nodeById(this.db, id);
    if (node && node.is_active === 1) {
      node.is_active = 0;   // 只改标志位，数据保留
      n += 1;
    }
  }
  return n;  // 返回本次软删数量
}
```

API 返回值 `{ deactivated: N }` 告知调用方本次影响了多少节点，便于前端提示用户。

---

## 亮点四：移动节点的正确性保障

### 是什么

`move()` 操作支持将任意节点（连同其子树）挂载到另一个父节点下，同时保证闭包表和物化路径的完全正确。

### 为什么这是难点

移动操作需要同时维护三件事：

1. **闭包表的祖先-后代关系** — 必须断开与旧父链路的连接，建立与新父链路的连接
2. **path / path_name** — 节点及其所有子孙的路径都要更新
3. **防止非法移动** — 不能移到自身下、不能移到自己的子孙下（否则形成环）

### 安全检查

```typescript
// treeService.ts — move()
if (nodeId === newParentId) throw new Error("不能将节点移动到自身下");

const sub = subtreeIds(this.db, nodeId);
if (sub.has(newParentId)) throw new Error("不能将节点移动到其子树内");
// 若 newParentId 在被移动节点的子树内，则会形成环，直接拒绝
```

### 闭包表重建逻辑（核心三步）

```
第一步：断边
  删除所有"外部祖先 → 子树内节点"的闭包行
  （子树内部互相之间的关系不变）

第二步：接边
  遍历"新父节点的所有祖先（含自身）" × "被移动子树的所有节点"
  两两配对，写入新闭包行

第三步：更新物化路径
  对子树内所有节点，计算新的 path / path_name / depth
```

---

## 亮点五：前端懒加载 + 受控 loadedKeys

### 是什么

前端 Ant Design Tree 组件采用**按需加载**策略：用户点击展开某节点时，才发起 HTTP 请求拉取其子节点，不会一次性加载整棵树。

### 为什么

如果树有几千个节点，一次性拉全量数据会：
- 首屏慢（等待时间长）
- 内存占用大
- 服务端压力大

懒加载将数据拉取时机分散到用户的实际交互操作中，**用户只看自己展开的部分，其余不加载**。

### 受控 loadedKeys 解决什么问题

Ant Design Tree 默认行为：一旦子节点加载过，就缓存在组件内部，不会再触发 `loadData`。但是当数据被增删改后，页面上显示的子节点列表已经过期了。

解决方案：**用 `loadedKeys` 受控接管 Ant Design 的缓存判断**，在任意增删改操作后将 `loadedKeys` 清空，使 Tree 对所有已展开节点重新发起加载请求，保证数据始终最新。

```typescript
// useAreaTree.ts — refreshRoots()
const refreshRoots = useCallback(async () => {
  const roots = await api.roots();
  setTreeData(roots.map(toTreeNode));
  setLoadedKeys([]);  // ← 清空已加载缓存，触发所有展开节点重新加载
}, []);
```

### has_children 字段的作用

后端在返回节点时，额外附带 `has_children` 字段（查 `parent_id` 索引即可）。
前端据此决定该节点是否显示展开箭头：

- `has_children = true` → 显示箭头，点击时触发懒加载
- `has_children = false` → 显示为叶子节点，不发请求

这样避免了"点开后发现是空的"这种体验问题。

---

---

## 亮点六：重命名节点的 path_name 级联更新

### 是什么

当用户对任意节点执行**重命名**操作时，该节点本身以及它的**所有后代节点**的 `path_name` 字段都会被自动同步更新，保证物化路径始终与树结构一致。

### 具体发生了什么

以当前演示数据为例，将"监控室"（id=2）重命名为"**中央监控室**"：

**重命名前：**

| id | name | path_name |
|----|------|-----------|
| 2  | 监控室 | 机电产业园/停车场/监控室 |
| 3  | 内监控室 | 机电产业园/停车场/**监控室**/内监控室 |
| 4  | 火灾监控 | 机电产业园/停车场/**监控室**/内监控室/火灾监控 |
| 7  | 给个 | 机电产业园/停车场/**监控室**/内监控室/火灾监控/给个 |
| 8  | 测试子节点这个 | 机电产业园/停车场/**监控室**/内监控室/火灾监控/给个/测试子节点这个 |

**重命名后（只改了 id=2 的名字，后代全部自动更新）：**

| id | name | path_name |
|----|------|-----------|
| 2  | 中央监控室 | 机电产业园/停车场/中央监控室 |
| 3  | 内监控室 | 机电产业园/停车场/**中央监控室**/内监控室 |
| 4  | 火灾监控 | 机电产业园/停车场/**中央监控室**/内监控室/火灾监控 |
| 7  | 给个 | 机电产业园/停车场/**中央监控室**/内监控室/火灾监控/给个 |
| 8  | 测试子节点这个 | 机电产业园/停车场/**中央监控室**/内监控室/火灾监控/给个/测试子节点这个 |

一次重命名操作，后台同步更新了 **5 条记录**的 `path_name`，调用方完全无感知。

### 代码实现

```typescript
// treeService.ts — renameNode()
renameNode(nodeId: number, newName: string): AreaNode {
  const node = activeNode(this.db, nodeId);
  const parent = node.parent_id != null ? activeNode(this.db, node.parent_id) : null;

  const oldPrefix = node.path_name;           // "机电产业园/停车场/监控室"
  node.name = newName;
  node.path_name = parent
    ? `${parent.path_name}/${newName}`          // "机电产业园/停车场/中央监控室"
    : newName;

  const oldPrefixSlash = `${oldPrefix}/`;      // "机电产业园/停车场/监控室/"
  const newPrefixSlash = `${node.path_name}/`; // "机电产业园/停车场/中央监控室/"

  // 遍历所有节点，凡是 path_name 以旧前缀开头的，一律替换前缀
  for (const n of this.db.data.area_nodes) {
    if (n.id === nodeId || n.is_active !== 1) continue;
    if (n.path_name.startsWith(oldPrefixSlash)) {
      n.path_name = newPrefixSlash + n.path_name.slice(oldPrefixSlash.length);
    }
  }
  return node;
}
```

**关键点：** 用字符串前缀替换而不是逐节点递归查询——只需一次全表扫描，时间复杂度 O(N)，N 为节点总数，与树深度无关。

### 前端实时预览

重命名 Modal 在用户输入新名称时，会**实时展示路径变化预览**：

- 上方灰色区域：展示当前节点的完整 `path_name`（只读）
- 下方蓝色区域：实时计算并预览修改后的路径，最后一段标红表示已变更
- 底部提示：明确告知"该节点的所有后代 path_name 将同步级联更新"

这样演示时可以在输入框里打字，路径预览实时刷新，直观展示级联更新的范围。

### 设计权衡

| | 方案A（每次读时计算路径） | 方案B（物化 path_name + 写时级联）|
|--|--|--|
| 读路径 | O(depth) 次查询 | O(1) 直读字段 |
| 写（重命名） | 直接更新一条记录 | 级联更新所有后代 |
| 适用场景 | 读少写多 | **读多写少**（区域结构稳定，查询频繁） |

本项目选择方案B，符合区域管理系统的实际使用模式：结构基本固定，但大量业务模块需要读取区域的完整路径。

---

## 一句话总结（适合汇报收尾）

> 这套系统的核心思路是**"以写换读"**：
> 写入时多做一点计算（维护闭包表、物化 path_name），
> 换来查询时的零递归、O(1) 路径读取、以及任意深度子树的一次扫描获取。
> 同时通过软删除和移动合法性校验，保证数据的安全性和一致性。
